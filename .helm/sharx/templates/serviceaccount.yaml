{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sharx.serviceAccountName" . }}
  labels:
    {{- include "sharx.labels" . | nindent 4 }}
{{- end }}
